# Agent Guidelines

## Pre-commit Hooks

Install [prek](https://github.com/eficode/prek) to manage pre-commit hooks:

```bash
uvx prek install
```

This ensures linting, formatting, and tests run automatically before each commit.
